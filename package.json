{
  "name": "sammedcode-platform-template",
  "version": "1.0.0",
  "description": "A modern full-stack platform template with monorepo structure",
  "private": true,
  "scripts": {
    "dev": "echo 'Start development servers for all apps. Implement with: concurrently \"npm run dev --workspace=apps/frontend\" \"npm run dev --workspace=apps/backend\"'",
    "dev:frontend": "npm run dev --workspace=apps/frontend",
    "dev:backend": "npm run dev --workspace=apps/backend",
    "build": "echo 'Build all applications. Implement with: npm run build --workspaces'",
    "build:frontend": "npm run build --workspace=apps/frontend",
    "build:backend": "npm run build --workspace=apps/backend",
    "test": "echo 'Run tests for all apps. Implement with: npm run test --workspaces'",
    "test:frontend": "npm run test --workspace=apps/frontend",
    "test:backend": "npm run test --workspace=apps/backend",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "eslint . --ext .ts,.tsx,.js,.jsx --fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "template",
    "platform",
    "monorepo",
    "full-stack"
  ],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "libs/*"
  ]
}
