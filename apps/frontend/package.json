{
  "name": "@sammedcode/frontend",
  "version": "1.0.0",
  "description": "Frontend application for SammedCode Platform",
  "private": true,
  "scripts": {
    "dev": "echo 'Start development server. Implement with your framework: react-scripts start | next dev | vite'",
    "build": "echo 'Build for production. Implement with your framework: react-scripts build | next build | vite build'",
    "start": "echo 'Start production server. Implement with your framework: serve -s build | next start'",
    "test": "echo 'Run tests. Implement with: jest | vitest'",
    "test:watch": "echo 'Run tests in watch mode'",
    "test:coverage": "echo 'Run tests with coverage'",
    "lint": "eslint src --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "eslint src --ext .ts,.tsx,.js,.jsx --fix",
    "format": "prettier --write \"src/**/*.{ts,tsx,js,jsx,json,css}\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {}
}
